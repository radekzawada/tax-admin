pl:
  dry_validation:
    errors:
      filled?: "pole nie może być puste"
      int?: "wartość musi być liczbą całkowitą"
      size?: "wartość musi mieć rozmiar {size}"
      format?: "wartość ma nieprawidłowy format"
      type?: "wartość musi być typu {type}"
      included_in?: "wartość musi być jednym z: {values}"
      excluded?: "wartość nie może być jednym z: {values}"
      key?: "pole musi być obecne"
      max_size?: "wartość nie może być większa niż {max}"
      min_size?: "wartość musi mieć co najmniej {min}"
      confirmed?: "potwierdzenie nie pasuje"
      unique?: "wartość musi być unikalna"
      between?: "wartość musi być między {min} a {max}"
      after?: "wartość musi być po {date}"
      before?: "wartość musi być przed {date}"
      not_found?: "obiect nie został znaleziony"
